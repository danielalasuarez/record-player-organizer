<ul class="right">
  <li><a href="/records"><img class= "logo" src="https://i.imgur.com/SfCKKO1.png" alt="LOGO"></a></li>
    <li><a href="/records/new">Add a New Record To Your Collection</a></li>
    <% if (currentUser) { %>
    <li>Welcome <%= currentUser.username %></li>
    <li>
      <form action="/sessions?_method=DELETE" method="POST">
        <input type="submit" value="Log Out" class="btn-small" />
      </form>
    </li>
    <% } else { %>
      <!-- this is the link to the new route creates a new user and will show up on console -->
    <li><a href="/users/new">Sign Up</a></li> 
    <!-- this will link back to sessions which is triggered when the username and pword are found in the database -->
    <li><a href="/sessions/new">Log In</a></li>
    <% } %>
  </ul>

  <style>
    nav {
      background-color: #1DB954;
      color: white;
    }
    body {
      background-color: #191414;
      color: white;
    }
    .logo{
      position: absolute;
    width: 240px;
    top: -72px;
    left: -5px;
    }
    .btn-small {
      background-color: white;
      color: #191414;
      border: #191414;
      border-radius: 6px;
      margin-left: 12px;
      margin-right: 12px;
    }
    .btn-small:hover{
      background-color: #191414;
      color: white;
      border: white;
      font-weight: bolder;
      cursor: pointer;
    }
  </style>
